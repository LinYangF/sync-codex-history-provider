interface:
  display_name: "Fix Missing Codex History"
  short_description: "Restore missing Codex history after provider changes"
  default_prompt: "Sync my missing Codex history after changing API provider."
